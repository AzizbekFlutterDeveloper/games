import 'package:flutter/widgets.dart';

List containers = [0, 0, 0, 0, 0, 0, 0, 0, 0];
int navbati = 0;

String win = 'Tic Tac Toe';

class Data extends Object {
  Uri image = Uri.parse("assets/transparent.png");
  int son = 0;

  Data(this.image, this.son);
}
